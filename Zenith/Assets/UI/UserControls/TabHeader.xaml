<helpers:ActivatableUserControl xmlns:helpers="clr-namespace:Zenith.Assets.UI.Helpers"
        x:Class="Zenith.Assets.UI.UserControls.TabHeader"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:Zenith.Assets.UI.UserControls"
        mc:Ignorable="d" x:Name="owner"
        d:DesignHeight="40" d:DesignWidth="150">
    <Border x:Name="headerBorder" Background="Transparent" BorderBrush="Silver" BorderThickness="0" MinWidth="120">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Title}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8,0"/>
            <TextBlock Text="×" VerticalAlignment="Center" FontSize="18" Grid.Column="1" Margin="8,0"/>
        </Grid>
    </Border>
</helpers:ActivatableUserControl>
